<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CollectorApp.Views.AddItem"
             Title="AddItem">
    <VerticalStackLayout>
        <CollectionView x:Name="itemValuesCollection"
         ItemsSource="{Binding ItemTypes}"
         Margin="20"
         SelectionMode="Single"
         >


            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding}"/>
                        <Editor HeightRequest="10" Placeholder="Enter value" x:Name="{Binding}"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <HorizontalStackLayout>
            <Button Text="Add item" Clicked="Add_Item"/>
            <Button Text="Return" Clicked="ReturnButtonClicked" />
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>